import 'package:flutter/material.dart';

class AsosiyScreen extends StatelessWidget {
  const AsosiyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}